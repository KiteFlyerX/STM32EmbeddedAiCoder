# EmbeddedAiCoder 运行依赖
# 安装:pip install -r requirements.txt

# GUI(Fluent Design)
PySide6>=6.5
PySide6-Fluent-Widgets>=1.5

# 采集 / 烧录
pyserial>=3.5
pyocd>=0.36

# AI API 调用(M1 占位)
httpx>=0.27

# 配置
pyyaml>=6.0
